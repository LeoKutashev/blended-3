const numbers = [2, 4, 6, 8, 10];

const isAllEven = numbers.every(num => num % 2 === 0);
//Евери проверяет каждый елемент масива на соответсвие условию
console.log(isAllEven); // Очікуваний результат: true
